name: Deploy

on:
  push:
    branches:
      -  master
  pull_request:
    branches:
      -  master

env:
  CI_SETTINGS__TUSK_VERSION: v0.5.1
  CI_SETTINGS__APP_DOCKER_REPO: docker.pkg.github.com/airtonix/phaser-tutorial-one

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Local bin path
        run: echo "::add-path::~/bin"

      - name: Install tusk
        run: >-
          curl -sL https://git.io/tusk | sh -s --
          -b ~/bin $CI_SETTINGS__TUSK_VERSION

      - name: Install envsub
        run: >-
          npm install -g envsub

      - name: Build ci image
        run: >-
          tusk ci:docker:build
          --appname=app
          --repo=$CI_SETTINGS__APP_DOCKER_REPO
          --version=$GITHUB_SHA

      - name: Rewrite Templates
        run: >-
          tusk docker:compose-templates
          --version=$GITHUB_SHA

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Update docker-compose files

      - name: Publish image
        run: >-
          tusk docker:publish
          --appname=app
          --tag=$CI_SETTINGS__APP_DOCKER_REPO:builder;
          tusk docker:publish
          --appname=app
          --tag=$CI_SETTINGS__APP_DOCKER_REPO:latest;
          tusk docker:publish
          --appname=app
          --tag=$CI_SETTINGS__APP_DOCKER_REPO:$GITHUB_SHA;

  ghpages:
