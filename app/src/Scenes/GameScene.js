import Phaser from 'phaser'

import {BaseScene} from './BaseScene'


export class GameScene extends BaseScene {
  constructor () {
    super({ key: 'Game' })
  }

  create () {

  }

  update () {
  }
}
