tasks:
  dev:
    run: >-
      tusk dev:serve

  dev:serve:
    run: >-
      npx parcel serve
      --hmr-port $HMR_PORT
      --port $PORT
      ./src/index.html

  test:
    run:
      - >-
        npx ava

  test:watch:
    run:
      - >-
        npx ava --watch
